service: email-service

frameworkVersion: ">=1.28.0 <2.0.0"

provider:
  name: aws
  runtime: go1.x
  iamRoleStatements:
    - Effect: "Allow"
      Action:
        - "ses:SendEmail"
      Resource: ["*"]

package:
 exclude:
   - ./**
 include:
   - ./bin/**

functions:
  sendMail:
    handler: bin/sendMail
    events:
      - http:
          path: sendMail
          method: post
          cors: true